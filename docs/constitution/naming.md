# 네임스페이스 규칙 — Kaelun

회의 #017 결정(2026-07-28) 실행. 이름을 어디에 어떻게 쓰는지 한 곳에 정한다.

---

## 확정된 이름

**회사명은 Kaelun이다.** 뜻은 없다 — 특정 어원 없는 순수 조어이고,
"뜻을 고정하지 않은 것 자체가 아직 아무것도 정해지지 않은 회사라는 컨셉의 실천"이 근거다.

| 층위 | 이름 | 쓰는 곳 |
|---|---|---|
| 회사 | **Kaelun** | 법인·도메인·GitHub 조직·패키지 |
| 창립 서사·IP | **Project Freedom** | 공개 기록·연재·콘텐츠 |
| 제품 | **Tous** | AI Company Operating System |
| 제품 | **Factory** | AI 영상 자동화 도구 |

---

## 레지스트리별 사용명 (2026-07-28 실측 기준)

| 곳 | 사용할 이름 | 상태 |
|---|---|---|
| 도메인 (.com/.ai/.io/.co) | `kaelun` | 미등록 — **CEO 실결제 필요** |
| GitHub 조직 | `kaelun` | 사용 가능 |
| npm | `kaelun` | 사용 가능 |
| PyPI | `kaelun` | 사용 가능 |
| **Docker Hub** | **`kaelun-hq`** | 원래 이름 선점됨 → 접두사 우회 |

**Docker Hub만 다른 이유**: `kaelun` 계정이 이미 존재한다.
회의 #017에서 "선점된 곳만 접두사 변형으로 국소 우회한다"고 결정했다 —
회사명 자체를 바꾸지 않는다. 웹사이트·이메일·저장소가 전부 `kaelun`이므로
브랜드 정체성은 이 하나로 흔들리지 않는다.

---

## 새 이름을 만들 때

제품이나 저장소 이름을 새로 지을 때는 **회사명을 접두사로 쓰지 않는다.**
`kaelun-tous`가 아니라 `tous`다 — 조직 아래 있으면 이미 소속이 드러난다.

예외는 외부 레지스트리에 올릴 때다. npm·PyPI처럼 전역 네임스페이스를 쓰는 곳에서는
`@kaelun/tous` 형태의 스코프를 쓴다.

---

## 아직 안 정한 것

**상표 출원 여부와 시점** — 회의 #017에서 "안 한다"가 아니라
"이 안건 범위 밖"으로 정리됐다. 언제 다시 상정할지는 미정이며, CEO 판단 대기다.
