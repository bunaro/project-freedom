# Tous Remote (텔레그램 리모컨) 설정 — 이 파일을 .env 로 복사해 값을 채우세요.
# .env 는 절대 커밋 금지.

# 1) @BotFather 에게 /newbot 으로 봇 생성 후 받은 토큰
TELEGRAM_BOT_TOKEN=여기에_봇토큰

# 2) 처음엔 비워두고 remote.py 실행 → 폰에서 아무 메시지나 보내면
#    콘솔/답장으로 채팅 ID가 표시됩니다. 그 값을 여기 넣고 재시작하세요.
#    (등록된 채팅 외에는 무응답 = 타인 접근 차단)
TOUS_CHAT_ID=
